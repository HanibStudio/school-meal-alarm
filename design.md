# 급식 알리미 - Design Document

## Brand Identity
- **앱 이름**: 급식 알리미
- **컨셉**: 깔끔하고 식욕을 돋우는 따뜻한 느낌의 디자인
- **Primary Color**: `#FF6B35` (따뜻한 오렌지 - 식사/음식 연상)
- **Background**: `#FFFFFF` / Dark: `#151718`
- **Surface**: `#FFF8F5` / Dark: `#1e2022`
- **Accent**: `#4CAF50` (초록 - 신선함, 건강)

## Screen List

### 1. 홈 화면 (Home)
- 오늘 날짜 표시
- 선택된 학교 이름 표시
- 오늘의 급식 메뉴 카드 (조식/중식/석식 탭)
- 알림 상태 배지 (켜짐/꺼짐)
- 학교 미선택 시 → 학교 검색 유도 배너

### 2. 학교 검색 화면 (SchoolSearch)
- 검색창 (학교 이름 입력)
- 검색 결과 목록 (학교명, 지역, 학교 종류)
- 학교 선택 → 홈으로 이동

### 3. 설정 화면 (Settings)
- 선택된 학교 정보 + 변경 버튼
- 알림 ON/OFF 토글
- 알림 시간 설정 (시/분 선택 - 기본: 오전 7:30)
- 급식 종류 선택 (조식/중식/석식 체크박스)
- 앱 정보

## Key User Flows

### 최초 실행 플로우
1. 홈 화면 → 학교 미선택 상태 배너 표시
2. "학교 검색" 버튼 탭 → 학교 검색 화면
3. 학교 이름 입력 → 검색 결과 표시
4. 학교 선택 → 홈 화면으로 복귀, 오늘 급식 표시

### 알림 설정 플로우
1. 하단 탭 "설정" 탭
2. 알림 토글 ON
3. 알림 시간 탭 → 시간 선택 모달
4. 확인 → 매일 해당 시간에 알림 예약

### 급식 조회 플로우
1. 홈 화면에서 날짜 좌우 스와이프 또는 화살표로 날짜 변경
2. 조식/중식/석식 탭 전환
3. 메뉴 목록 표시 (알레르기 정보 포함)

## Layout Design

### 홈 화면 레이아웃
```
┌─────────────────────────┐
│  [학교명]          [설정] │  ← 헤더
├─────────────────────────┤
│  < 2026년 5월 15일 목 >  │  ← 날짜 네비게이터
├─────────────────────────┤
│  [조식] [중식] [석식]     │  ← 식사 탭
├─────────────────────────┤
│  🍚 오늘의 급식           │
│  ┌───────────────────┐  │
│  │ 현미찹쌀밥          │  │
│  │ 배추된장국 (5.6.13) │  │
│  │ 오이배생채 (13)     │  │
│  │ 쇠불고기 (5.6.13)  │  │
│  └───────────────────┘  │
│  칼로리: 850 kcal        │
└─────────────────────────┘
│ [🏠 홈]    [⚙️ 설정]    │  ← 탭 바
```

### 학교 검색 화면
```
┌─────────────────────────┐
│  ← 학교 검색             │  ← 헤더
├─────────────────────────┤
│  [🔍 학교 이름 검색...]   │  ← 검색창
├─────────────────────────┤
│  ┌───────────────────┐  │
│  │ 서울중학교          │  │
│  │ 서울특별시 · 중학교  │  │
│  ├───────────────────┤  │
│  │ 서울고등학교        │  │
│  │ 서울특별시 · 고등학교│  │
│  └───────────────────┘  │
└─────────────────────────┘
```

## Color Tokens (theme.config.js)
- `primary`: `#FF6B35` (오렌지)
- `background`: `#FFFFFF` / `#151718`
- `surface`: `#FFF8F5` / `#1e2022`
- `foreground`: `#1A1A1A` / `#ECEDEE`
- `muted`: `#888888` / `#9BA1A6`
- `border`: `#F0E8E4` / `#334155`
- `success`: `#4CAF50` / `#4ADE80`
- `warning`: `#F59E0B` / `#FBBF24`
- `error`: `#EF4444` / `#F87171`
